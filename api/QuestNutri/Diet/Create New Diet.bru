meta {
  name: Create New Diet
  type: http
  seq: 1
}

post {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/diet/new
  body: json
  auth: inherit
}

body:json {
  {
    "patientId": "{{patientId}}",
    "endDate": "2025-11-15"
  }
}

vars:pre-request {
  patientId: {{PATIENT_ID}}
}

script:post-response {
  if(res.status === 201 && res("id")) {
    bru.setVar("createdDiet" ,res("id"));
  }
  
  if(res.status !== 201) {
      bru.setNextRequest(null);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
