meta {
  name: Diet plan
  type: http
  seq: 3
}

get {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/diet/plan?length=24
  body: json
  auth: bearer
}

params:query {
  length: 24
}

auth:bearer {
  token: {{PATIENT_ACCESS_TOKEN}}
}

body:json {
  {
    "patientId": "{{PATIENT_ID}}",
    "nutritionistId": "{{NUTRITIONIST_ID}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
