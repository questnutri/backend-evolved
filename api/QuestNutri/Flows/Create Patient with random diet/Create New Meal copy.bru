meta {
  name: Create New Meal copy
  type: http
  seq: 3
}

post {
  url: http://{{GATEWAY_URL}}/diet/{{createdDiet}}/meal
  body: json
  auth: bearer
}

auth:bearer {
  token: {{NUTRITIONIST_ACCESS_TOKEN}}
}

body:json {
  {
    "name": "Breakfast"
  }
}

script:post-response {
  if(res.status === 201 && res("id")) {
    bru.setVar("createdMeal" ,res("id"));
  }
  
  if(res.status !== 201) {
      bru.setNextRequest(null);
  }
}

settings {
  encodeUrl: true
}
